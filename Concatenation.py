# we are going to take the name of the user and see if he matches the name on file or not
name = input("what is your name: ")

# now we are going to use an if statment to test
# we use + for string concatenation
if name == "Abdul" :
  print (name + " is allowed")

else:
  print (name + " is not allowd")